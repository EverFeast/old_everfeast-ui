import React from 'react';
import ProgressiveWrapper from "../layout/ProgressiveWrapper";

function Search({}) {
    return (
        <ProgressiveWrapper title="Recipe Search">
        </ProgressiveWrapper>
    );
}

export default Search;
